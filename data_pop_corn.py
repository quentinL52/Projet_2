import streamlit as st
import pandas as pd
st.set_page_config(layout="wide")
from htbuilder import HtmlElement, div, ul, li, br, hr, a, p, img, styles, classes, fonts

col1, col2 = st.columns(2)
with col1:
    st.image("Design sans titre.png")

with col2:
    st.text("")
    st.text("")
    st.text("")
    st.text("")
    st.text("")
    st.text("")
    st.write("")